import fs from 'fs';

const path = 'presentation/src/app/explorer/explorer.model.ts';
let s = fs.readFileSync(path, 'utf8');
const start = s.indexOf("  // ─── Part 4 · Angular client");
const end = s.lastIndexOf('];');
if (start < 0 || end < 0) {
  console.error('markers', start, end);
  process.exit(1);
}

const part4 = `  // ─── Part 4 · Angular client ──────────────────────────────────
  {
    id: 'ch4',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'Overview',
    title: 'What is Angular?',
    narration: 'App-like websites — pages update instantly, like Gmail or Netflix, without a full reload.',
    takeaway: 'Components · Services · Routing · RxJS',
    stage: 'ui',
    uiMock: 'overview',
    example:
      'CineTrack feels like one app: click Discover or Watchlist and only the main panel changes — the sidebar stays put.',
    bullets: [
      'Reusable UI pieces (components)',
      'TypeScript catches mistakes early',
      'Structure built for long-lived apps',
    ],
  },
  {
    id: 'ng-pillars',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'Core ideas',
    title: 'Four pillars',
    narration: 'Everything in Angular revolves around these building blocks.',
    takeaway: 'Learn these four first',
    stage: 'ui',
    uiMock: 'pillars',
    example:
      'In CineTrack: MovieCard (component) + WatchlistService (service) + /watchlist (routing) + HTTP Observables (RxJS).',
    ideas: ANGULAR_IDEAS.pillars,
  },
  {
    id: 'ng-component',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'Components',
    title: 'How a component works',
    narration: 'Template + styles + TypeScript logic — write once, reuse on many pages.',
    takeaway: 'Same MovieCard, four screens',
    stage: 'ui',
    uiMock: 'component',
    example:
      'MovieCardComponent is shared: Dashboard row, Discover grid, Watchlist, and Favorites all render the same card.',
    ideas: ANGULAR_IDEAS.component,
    snippet: SNIPPETS.ngMovieCard,
    focusLines: [1, 2, 3],
    treePath: ['client', 'src', 'app', 'shared', 'components', 'movie-card'],
  },
  {
    id: 'ng-signals',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'Signals',
    title: 'Signals — watched values',
    narration: 'A signal is a value the screen watches. When it changes, every reader updates automatically.',
    takeaway: 'signal() + computed()',
    stage: 'ui',
    uiMock: 'signals',
    example:
      'Add Dune to the watchlist → items signal updates → the sidebar badge jumps from 3 to 4 with no manual DOM code.',
    bullets: [
      'Hold state in signal()',
      'Expose read-only views',
      'computed() derives values like isAuthenticated',
    ],
    snippet: SNIPPETS.ngSignals,
    focusLines: [1, 2, 4, 5],
    treePath: ['client', 'src', 'app', 'core', 'services', 'auth.service.ts'],
  },
  {
    id: 'ng-routing',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'Routing',
    title: 'Routing connects pages',
    narration: 'Click Watchlist → URL changes → Angular swaps the page — no full browser reload.',
    takeaway: 'URL ↔ component',
    stage: 'ui',
    uiMock: 'routing',
    example:
      'Sidebar Watchlist → browser shows /watchlist → WatchlistComponent loads lazily via loadComponent.',
    ideas: ANGULAR_IDEAS.routing,
    snippet: SNIPPETS.ngRoutes,
    focusLines: [1, 2, 3, 4, 5],
    treePath: ['client', 'src', 'app', 'app.routes.ts'],
  },
  {
    id: 'ng-guards',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'Guards',
    title: 'Route guards',
    narration: 'authGuard protects private pages. guestGuard keeps logged-in users off Login.',
    takeaway: 'Bouncers for routes',
    stage: 'ui',
    uiMock: 'guards',
    example:
      'Not signed in and open /watchlist → authGuard sends you to /login. Already signed in on /login → guestGuard sends you to /dashboard.',
    bullets: [
      'authGuard → must be signed in',
      'guestGuard → leave Login if already signed in',
      'Return true or redirect with UrlTree',
    ],
    snippet: SNIPPETS.ngAuthGuard,
    focusLines: [1, 2, 3, 4, 5],
    treePath: ['client', 'src', 'app', 'core', 'guards', 'auth.guard.ts'],
  },
  {
    id: 'ng-rxjs',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'RxJS',
    title: 'RxJS — work over time',
    narration: 'API calls return an Observable. Subscribe when the answer arrives — the UI stays responsive.',
    takeaway: 'Streams, not freezes',
    stage: 'ui',
    uiMock: 'rxjs',
    example:
      'On Discover, typing "dun" waits ~300ms after the last keypress, then one TMDb search fires — not a request per letter.',
    bullets: [
      'HTTP calls return Observables',
      'subscribe() reacts when data arrives',
      'Errors become toasts, not crashes',
    ],
    snippet: SNIPPETS.ngWatchlistLoad,
    focusLines: [4, 5, 6, 7, 8],
    treePath: ['client', 'src', 'app', 'features', 'watchlist'],
  },
  {
    id: 'ng-services',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'Services',
    title: 'Services & DI',
    narration: 'Components ask for AuthService or WatchlistService — Angular injects them automatically.',
    takeaway: 'Logic once, use everywhere',
    stage: 'ui',
    uiMock: 'services',
    example:
      'inject(WatchlistService) in MovieDetails and Watchlist pages — both share one HTTP + toast pattern. authInterceptor attaches the JWT on every call.',
    bullets: [
      'Services hold reusable logic',
      'Dependency Injection provides them',
      'Interceptors attach the JWT token',
    ],
    snippet: SNIPPETS.ngInterceptor,
    focusLines: [1, 2, 3, 4, 5, 6],
    treePath: ['client', 'src', 'app', 'core', 'interceptors'],
  },
  {
    id: 'ng-folders',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'Layout',
    title: 'client/src/app layout',
    narration: 'core = engine, shared = reusable UI, features = pages.',
    takeaway: 'Find any file in seconds',
    stage: 'folder',
    folderTree: 'client',
    folderVisible: ['client', 'src', 'app'],
    folderSpot: 'app',
    example:
      'Need the login screen? features/auth. Need MovieCard? shared/components. Need JWT attach? core/interceptors.',
  },
  {
    id: 'ng-folder-core',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'Layout',
    title: 'core/',
    narration: 'Models, services, guards, and interceptors — non-visual power behind every page.',
    takeaway: 'Engine room',
    stage: 'folder',
    folderTree: 'client',
    folderVisible: [
      'client',
      'src',
      'app',
      'app.routes.ts',
      'core',
      'core/models',
      'core/services',
      'core/guards',
      'core/interceptors',
    ],
    folderSpot: 'core',
    example: 'AuthService + authGuard + authInterceptor — login state is defined once and reused app-wide.',
  },
  {
    id: 'ng-folder-shared',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'Layout',
    title: 'shared/',
    narration: 'Reusable visuals — MovieCard, sidebar, toasts — written once, used on many pages.',
    takeaway: 'Reusable UI',
    stage: 'folder',
    folderTree: 'client',
    folderVisible: [
      'client',
      'src',
      'app',
      'app.routes.ts',
      'core',
      'core/models',
      'core/services',
      'core/guards',
      'core/interceptors',
      'shared',
      'shared/components',
      'shared/pipes',
    ],
    folderSpot: 'shared',
    example: 'Change MovieCard styling once — Dashboard, Discover, Watchlist, and Favorites all update.',
  },
  {
    id: 'ng-folder-features',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'Layout',
    title: 'features/',
    narration: 'One folder per screen. Add a feature without risking the rest of the app.',
    takeaway: 'Pages live here',
    stage: 'folder',
    folderTree: 'client',
    folderVisible: [
      'client',
      'src',
      'app',
      'app.routes.ts',
      'core',
      'core/models',
      'core/services',
      'core/guards',
      'core/interceptors',
      'shared',
      'shared/components',
      'shared/pipes',
      'features',
      'features/auth',
      'features/watchlist',
      'features/discover',
    ],
    folderSpot: 'features',
    example:
      'New Statistics page = new features/statistics folder + one lazy route — core and shared stay untouched.',
  },
  {
    id: 'ng-connect',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'Connect',
    title: 'How pieces connect',
    narration: 'Watchlist page — router, guard, service, Observable, signal, shared MovieCard.',
    takeaway: 'One real path through the client',
    stage: 'ui',
    uiMock: 'watchlist',
    example:
      'Watchlist click → authGuard → WatchlistService.getAll() → subscribe → items signal → grid of MovieCards with Plan to Watch / Watching badges.',
    ideas: ANGULAR_IDEAS.connect,
    snippet: SNIPPETS.ngWatchlistLoad,
    focusLines: [1, 4, 5, 6, 7],
    treePath: ['client', 'src', 'app', 'features', 'watchlist'],
  },
  {
    id: 'ng-add',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'Action',
    title: 'Add from movie details',
    narration:
      'MovieDetailsComponent calls WatchlistService.add() — same POST /api/v1/watchlist you traced on the server.',
    takeaway: 'Client action → Fastify route',
    stage: 'ui',
    uiMock: 'details',
    example:
      'On /movie/693134 (Dune: Part Two), tap + Add to Watchlist → WatchlistService.add() → toast "Added to watchlist".',
    bullets: [
      'Details page owns the button',
      'Service owns the HTTP call',
      'Toast reports success or failure',
    ],
    snippet: SNIPPETS.ngWatchlistAdd,
    focusLines: [1, 4, 5, 6, 7],
    treePath: ['client', 'src', 'app', 'features', 'movie-details'],
  },
  {
    id: 'finale',
    act: 4,
    actTitle: 'Part 4',
    sceneLabel: 'Done',
    title: 'Server + client',
    narration: 'Fastify composes the API. Angular composes the UI. One watchlist request ties them together.',
    takeaway: 'Boot the API. Guard the routes. Follow one request.',
    stage: 'finale',
  },
];
`;

s = s.slice(0, start) + part4 + '\n';
fs.writeFileSync(path, s);
console.log('ok');
